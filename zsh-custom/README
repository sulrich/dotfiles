# overview

this is an attempt to make the shell appropriately morph to the environment that
it's being run.  host and platform specific elements should be loaded
appropriately depending on the platform this is being run on. 

# mac os

the use of `unset GLOBAL_RCS` prevents the sourcing of the RC files from `/etc/`
on mac os.  this makes things much simpler.  

